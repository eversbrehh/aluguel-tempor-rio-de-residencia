// MS Tarefa — esqueleto.
// A fila `tarefas.eventos` já é declarada pela topologia do monolito
// e está pronta para receber `associacao.criada` e `associacao.encerrada`.
// A implementação completa do consumidor está prevista para a Sprint 3.
console.log('🧩 [ms-tarefa] placeholder — implementação prevista na Sprint 3.');
