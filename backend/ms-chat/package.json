{
    "name": "@lamd/ms-chat",
    "version": "0.0.1",
    "private": true,
    "description": "Microsserviço de Chat (esqueleto — Sprint 3).",
    "main": "src/server.ts",
    "scripts": {
        "dev": "ts-node-dev --respawn --transpile-only --exit-child src/server.ts",
        "typecheck": "tsc --noEmit -p tsconfig.json"
    },
    "devDependencies": {
        "@types/node": "^20.14.0",
        "ts-node-dev": "^2.0.0",
        "typescript": "^5.4.5"
    },
    "engines": {
        "node": ">=20"
    }
}